# Leave empty to use same-origin + Vite proxy in dev.
# For production, point at your backend, e.g. http://localhost:3000
VITE_API_URL=
# Optional: if your backend sets API_KEY, paste it here
# VITE_API_KEY=
